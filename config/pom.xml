<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>oodt-core</artifactId>
        <groupId>ai.mattmann.mnemosyne</groupId>
        <version>1.11.0</version>
        <relativePath>../core/pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>oodt-conf</artifactId>

  <!--
    Declared explicitly rather than inherited. Maven appends the module path to
    an inherited url and scm, publishing a 404 project url and an scm
    connection ending in ".git/<module>". The child.*.inherit.append.path
    attributes meant to suppress that are not honoured under Maven 3.9.
  -->
  <url>https://github.com/chrismattmann/mnemosyne</url>
  <scm>
    <connection>scm:git:https://github.com/chrismattmann/mnemosyne.git</connection>
    <developerConnection>scm:git:https://github.com/chrismattmann/mnemosyne.git</developerConnection>
    <url>https://github.com/chrismattmann/mnemosyne</url>
    <tag>HEAD</tag>
  </scm>
    <packaging>jar</packaging>
    <name>OODT - Configuration Management</name>
    <description>OODT project for distributed configuration management support</description>

    <dependencies>
        <dependency>
            <groupId>ai.mattmann.mnemosyne</groupId>
            <artifactId>cas-cli</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.curator</groupId>
            <artifactId>curator-recipes</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.curator</groupId>
            <artifactId>curator-test</artifactId>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j-impl</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
        </dependency>
    </dependencies>

    <build>
        <!--
          log4j2.xml is deployment configuration, not library configuration. The
          assembly copies it from here into etc/, and the start scripts point at
          it with -Dlog4j.configurationFile. Left in the jar as well, it sits on
          the classpath of every application depending on this module and log4j2
          silently adopts it. That is how the file manager's own CLI tools came to
          log DEBUG to SYSTEM_OUT, which corrupts the output of any tool whose
          stdout is piped, such as query-tool feeding product ids to DeleteProduct.
        -->
        <resources>
          <resource>
            <directory>${basedir}/src/main/resources</directory>
            <excludes>
              <exclude>log4j2.xml</exclude>
              <exclude>logging.properties</exclude>
            </excludes>
          </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.6</version>
                <configuration>
          <!-- Built for local use, not attached. These RADiX bundles are
               deployment archives rather than library artifacts, and each
               one is well over 100 MB. -->
          <attach>false</attach>
                    <descriptors>
                        <descriptor>src/main/assembly/assembly.xml</descriptor>
                    </descriptors>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-filemgr-resources</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/resources/examples/filemgr</outputDirectory>
                            <overwrite>true</overwrite>
                            <resources>
                                <resource>
                                    <directory>../filemgr/src/main/resources/examples</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                </resource>
                                <resource>
                                    <directory>../filemgr/src/main/resources</directory>
                                    <includes>
                                        <include>filemgr.properties</include>
                                        <include>logging.properties</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-resource-manager-resources</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/resources/examples/resmgr</outputDirectory>
                            <overwrite>true</overwrite>
                            <resources>
                                <resource>
                                    <directory>../resource/src/main/resources/examples</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                </resource>
                                <resource>
                                    <directory>../resource/src/main/resources</directory>
                                    <includes>
                                        <include>resource.properties</include>
                                        <include>logging.properties</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
