<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.mattmann.mnemosyne</groupId>
    <artifactId>oodt-core</artifactId>
    <version>1.11.0</version>
    <relativePath>../core/pom.xml</relativePath>
  </parent>
  <artifactId>cas-pge</artifactId>

  <!--
    Declared explicitly rather than inherited. Maven appends the module path to
    an inherited url and scm, publishing a 404 project url and an scm
    connection ending in ".git/<module>". The child.*.inherit.append.path
    attributes meant to suppress that are not honoured under Maven 3.9.
  -->
  <url>https://github.com/chrismattmann/mnemosyne</url>
  <scm>
    <connection>scm:git:https://github.com/chrismattmann/mnemosyne.git</connection>
    <developerConnection>scm:git:https://github.com/chrismattmann/mnemosyne.git</developerConnection>
    <url>https://github.com/chrismattmann/mnemosyne</url>
    <tag>HEAD</tag>
  </scm>
  <name>CAS PGE Adaptor Framework</name>
  <description>Allows data processing jobs not written in conformance with the
        PCS PGE (Production Generation Executive) interface to be run within the PCS.</description>
  
  <dependencies>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-collections</groupId>
      <artifactId>commons-collections</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-lang</groupId>
      <artifactId>commons-lang</artifactId>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.sf.saxon</groupId>
      <artifactId>saxon-dom</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-crawler</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-filemgr</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-metadata</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>cas-workflow</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.mattmann.mnemosyne</groupId>
      <artifactId>oodt-commons</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.velocity</groupId>
      <artifactId>velocity</artifactId>
    </dependency>
    <dependency>
      <groupId>org.easymock</groupId>
      <artifactId>easymock</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-beans</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.objenesis</groupId>
      <artifactId>objenesis</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <!--
      logging.properties is deployment configuration, not library configuration.
      The assembly copies it from here into etc/ and the start scripts point at
      it with -Djava.util.logging.config.file. Left in the jar as well it rides
      into any war bundling this module, where the container's JUL setup can
      pick it up: OODT-991 reported exactly that, verbose logs appearing because
      the libs carried their own configuration.
    -->
    <resources>
      <resource>
        <directory>${basedir}/src/main/resources</directory>
        <excludes>
          <exclude>logging.properties</exclude>
        </excludes>
      </resource>
    </resources>
    <testSourceDirectory>${basedir}/src/test/java</testSourceDirectory>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.6</version>
        <configuration>
          <!-- Built for local use, not attached. These RADiX bundles are
               deployment archives rather than library artifacts, and each
               one is well over 100 MB. -->
          <attach>false</attach>
          <descriptors>
            <descriptor>src/main/assembly/assembly.xml</descriptor>
          </descriptors>
          <archive>
            <manifest>
              <mainClass>org.apache.oodt.cas.pge.PGETask</mainClass>
            </manifest>
          </archive>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
      </plugin>
    <plugin><artifactId>maven-surefire-plugin</artifactId><version>2.22.2</version><configuration><argLine>--add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED</argLine></configuration></plugin></plugins>
  </build>
  <profiles>
    <profile>
      <id>audit</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>rat-maven-plugin</artifactId>
            <configuration>
              <excludes>
                <exclude>**/resources/examples/**</exclude>
              </excludes>
              <numUnapprovedLicenses>2</numUnapprovedLicenses>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>